'use client'

import Breadcrumb from "@/app/components/Card/BreadCrumb"


const BreadcrumbWrapper = ({ name }: { name: string }) => {
  return <Breadcrumb name={name} />
}

export default BreadcrumbWrapper